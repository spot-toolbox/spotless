function e = eq(p1,p2)
    error('eq not implemented.');
    
    
    % First test if (i,j) entries have same number of rows
    % Then test if corresponding blocks are identical.
    % Probably special case 0.
    
end
